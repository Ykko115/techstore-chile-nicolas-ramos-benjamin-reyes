spring.application.name=microservice
server.port=8080

spring.datasource.url=jdbc:postgresql://localhost:5432/techstore
spring.datasource.username=postgres
spring.datasource.password=Nykolas2111
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.sql.init.mode=always

security.jwt.secret=TechStoreJwtSecretKeyForHs256Signing_ChangeInProd2026
security.jwt.expiration-seconds=3600

# Auditoría asíncrona de inventario (Amazon SQS)
# En ECS Fargate se inyectan como variables de entorno AWS_REGION y SQS_AUDIT_QUEUE_URL.
# En LabRole no se requieren AWS_ACCESS_KEY_ID/SECRET: el SDK los toma del rol de la tarea.
aws.region=${AWS_REGION:us-east-1}
aws.sqs.audit-queue-url=${SQS_AUDIT_QUEUE_URL:}
